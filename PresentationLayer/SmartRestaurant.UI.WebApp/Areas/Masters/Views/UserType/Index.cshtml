@model IEnumerable<SmartRestaurant.Model.UserTypeModel>
<p>
    <a class="btn btn-primary btn-sm" href="@Url.Action("Create", "UserType" , new { Area = "Masters" })">
        <i class="fa fa-lg fa-fw fa-plus"></i>
        Add
    </a>
</p>
<table class="table table-striped table-bordered">
    <tr>
        <th width="10%">
        </th>
        <th width="90%">
            @Html.DisplayNameFor(model => model.User_Name)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <a class="btn btn-xs btn-success" title="Edit" href="@Url.Action("Edit", "UserType" , new { Area = "Masters", code=item.User_Id })" data-ajax-update="#mainContent" data-ajax-mode="replace" data-ajax-method="GET" data-ajax="true">
                    <i class="fa fa-fw fa-edit"></i>
                </a>
                <a class="btn btn-xs btn-danger" title="Delete" href="@Url.Action("Delete", "UserType" , new { Area = "Masters", code=item.User_Id })" data-ajax-update="#mainContent" data-ajax-mode="replace" data-ajax-method="GET" data-ajax="true">
                    <i class="fa fa-fw fa-ban"></i>
                </a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User_Name)
            </td>
        </tr>
    }
</table>
